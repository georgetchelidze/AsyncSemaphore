# AsyncSemaphore

AsyncSemaphore is a tiny Swift package that provides an async-friendly semaphore for structured concurrency.

## Usage

Add `AsyncSemaphore` as a dependency in your Swift package and import it where needed.
